name: Terraform Provider OpenRouter

#logo: logo/logo.jpg
badges:
  - name: Latest Release
    image: https://img.shields.io/github/release/cloudopsworks/terraform-provider-openrouter.svg?style=for-the-badge
    url: https://github.com/cloudopsworks/terraform-provider-openrouter/releases/latest
  - name: Terraform Registry
    image: https://img.shields.io/badge/registry-cloudopsworks%2Fopenrouter-844FBA?style=for-the-badge&logo=terraform
    url: https://registry.terraform.io/providers/cloudopsworks/openrouter/latest
  - name: OpenTofu
    image: https://img.shields.io/badge/OpenTofu-compatible-FFDA18?style=for-the-badge&logo=opentofu&logoColor=black
    url: https://opentofu.org
  - name: Go Report Card
    image: https://goreportcard.com/badge/github.com/cloudopsworks/terraform-provider-openrouter?style=for-the-badge
    url: https://goreportcard.com/report/github.com/cloudopsworks/terraform-provider-openrouter
  - name: Last Updated
    image: https://img.shields.io/github/last-commit/cloudopsworks/terraform-provider-openrouter.svg?style=for-the-badge
    url: https://github.com/cloudopsworks/terraform-provider-openrouter/commits
  - name: Stars
    image: https://img.shields.io/github/stars/cloudopsworks/terraform-provider-openrouter.svg?style=for-the-badge
    url: https://github.com/cloudopsworks/terraform-provider-openrouter/stargazers

license: "APACHE2"

copyrights:
  - name: "Cloud Ops Works LLC"
    url: "https://cloudopsworks.co"
    year: "2021"

github_repo: cloudopsworks/terraform-provider-openrouter

description: |-
  Govern your entire OpenRouter organization as code — workspaces, guardrails,
  spend limits, and API keys. A Terraform/OpenTofu provider for the OpenRouter
  management API, not just an API-key wrapper.

introduction: |-
  OpenRouter now routes 20+ trillion tokens per week and has become shared
  infrastructure for most AI teams. Once it is a shared dependency, you need to
  manage it like one: scoped keys, spend caps, guardrails, and organization
  membership — versioned, reviewed in pull requests, and reproducible across
  environments.

  This is the only Terraform/OpenTofu provider that manages OpenRouter as a
  governed platform rather than a bare key vault.

  ### Why this provider

  | Capability | `cloudopsworks/openrouter` | Key-only providers |
  |---|:--:|:--:|
  | API keys (CRUD, spend / time limits) | ✅ | ✅ |
  | Workspaces | ✅ | ❌ |
  | Guardrails | ✅ | ❌ |
  | Organization / members | ✅ | ❌ |
  | Providers data source | ✅ | ❌ |
  | OpenTofu support | ✅ | rarely |

  ### Resources

  - `openrouter_api_key` — keys with spend and time limits
  - `openrouter_workspace`
  - `openrouter_guardrail`

  ### Data sources

  - `openrouter_workspace`
  - `openrouter_workspaces`
  - `openrouter_api_keys`
  - `openrouter_guardrails`
  - `openrouter_organization`
  - `openrouter_providers`

quickstart: |-
  ## Requirements

  - Go `1.25+`
  - Terraform `>= 1.7`
  - OpenTofu `>= 1.7`
  - OpenRouter management API key

  ## Installation

  ### Terraform

  ```hcl
  terraform {
    required_providers {
      openrouter = {
        source  = "cloudopsworks/openrouter"
        version = "~> 0.1"
      }
    }
  }
  ```

  ### OpenTofu

  After the provider is published to the OpenTofu Registry, use the same namespace/type pair:

  ```hcl
  terraform {
    required_providers {
      openrouter = {
        source  = "cloudopsworks/openrouter"
        version = "~> 0.1"
      }
    }
  }
  ```

usage: |-
  ## Provider configuration

  ```hcl
  provider "openrouter" {
    api_key = var.openrouter_management_key
  }
  ```

  You can also set `OPENROUTER_API_KEY` instead of `api_key`.

  Optional arguments:

  - `api_key` - OpenRouter management API key.
  - `base_url` - Override the management API base URL. Defaults to `https://openrouter.ai/api/v1`.

  ## Govern a team's access (not just a bare key)

  ```hcl
  # Scope a workspace, attach a guardrail, then issue a spend-capped key.
  resource "openrouter_workspace" "search_team" {
    name = "Search Team"
    slug = "search-team"
  }

  resource "openrouter_guardrail" "search_team" {
    name              = "search-team"
    workspace_id      = openrouter_workspace.search_team.id
    limit_usd         = 200 # monthly USD cap
    reset_interval    = "monthly"
    allowed_providers = ["openai", "anthropic"]
    enforce_zdr       = true
  }

  resource "openrouter_api_key" "search_team" {
    name         = "search-team-prod"
    workspace_id = openrouter_workspace.search_team.id
    limit        = 200 # USD spend cap
    limit_reset  = "monthly"
  }
  ```

  See [`examples/governance/`](examples/governance/) for a multi-team version that
  governs every team from a single `for_each`.

  ## Development

  ```sh
  make fmt
  make lint
  make test
  make build
  ```

  For acceptance tests:

  ```sh
  make testacc
  ```

examples: |-
  See:

  - `examples/provider/`
  - `examples/resources/`
  - `examples/data-sources/`
  - `docs/`

  ## Known limitations in v1

  - OpenRouter returns the actual API key secret only on create. The provider exposes it as `key` and marks it sensitive, but it can still exist in state.
  - `openrouter_organization` is implemented from the documented organization-members endpoint.

  ## Registry publishing

  This repository includes HashiCorp scaffolding-style release packaging for Terraform Registry/OpenTofu Registry publication:

  - `.goreleaser.yml`
  - `.github/workflows/release.yml`
  - `terraform-registry-manifest.json`

  Release details and the remaining registry-side manual steps are documented in [`RELEASING.md`](./RELEASING.md).

# Uncomment to include terraform generated docs (optional)
#include:
#  - "docs/targets.md"
#  - "docs/terraform.md"

contributors:
  - name: "Cristian Beraha"
    github: "berahac"
  - name: "Cloud Ops Works Dev Bot"
    github: "cloudopsworks-dev"
