organization_name: "CloudOpsWorks"
organization_unit: "Providers"
environment_name: "terraform-provider-openrouter"
repository_owner: "cloudopsworks"

golang:
  version: "1.24"
  main_file: .

semgrep:
  enabled: true

is_library: true

cloud: aws
cloud_type: lambda
